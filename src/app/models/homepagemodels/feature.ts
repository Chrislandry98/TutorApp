export interface Feature {
  titre: string;
  description: string;
  icone: string; // Nom de l'icône (ex: 'shield-check')
  couleur: string; // Classe CSS pour la couleur de l'icône (ex: 'bg-primary')
}
